{% extends 'base.html.twig' %}

{% block title %}Documents list{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>




    <div class="col-lg-12">

        <div class="ibox ">
            <div class="ibox-content">
                <div class="row">

                    <div class="col-sm-9">
                        <a href="{{ path('document.add') }}" class="btn btn-primary"><i class="fa fa-envelope"></i> Dodaj dokument </a>
                    </div>
                    <div class="col-sm-3">
                        <div class="input-group"><input placeholder="Szukaj" type="text" class="form-control form-control-sm"> <span class="input-group-append"> <button type="button" class="btn btn-sm btn-primary"><i class="fa fa-search"></i>
                                    </button> </span></div>

                    </div>
                </div>
            </div>
            <div class="ibox-content">

                <div class="table-responsive">

                    <div class="pull-left" style="width:200px;margin-bottom: 10px;">
                        <span style="display:inline-block">Pokaż</span>
                        <div style="display:inline-block">
                            <select class="form-control" style="height: 22px; font-size: 12px; padding: 0px 0px; display: inline-block;">
                                <option>25</option>
                                <option>50</option>
                                <option>100</option>
                                <option>250</option>
                                <option>500</option>
                            </select>
                        </div>
                        pozycji z 1034
                    </div>

                    <div class="pull-right">
                        <div class="btn-group">
                            <button type="button" class="btn btn-white btn-xs"><< Pierwsza</button>
                            <button class="btn btn-white btn-xs">< Poprzednia</button>
                        </div>
                        <div class="btn-group form-inline" style="display:inline-block; font-size: 12px; margin-top: 0px;">
                            Strona:
                            <input type="text" class="form-control" value="5" style="height: 20px; width: 40px; font-size: 12px; padding: 0px 3px; text-align: center;"> z 34
                        </div>
                        <div class="btn-group">
                            <button type="button" class="btn btn-white btn-xs">Następna ></button>
                            <button class="btn btn-white btn-xs">Ostatnia >></button>
                        </div>
                    </div>


                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th>Data</th>
                            <th>Nazwa</th>
                            <th>Numer dokumentu</th>
                            <th>Typ dokumentu</th>
                            <th></th>
                        </tr>
                        </thead>
                        <tbody>

                        {% for document in documents %}
                        <tr>
                            <td class="td-padding">{{ document.addDate|date("d.m.Y") }}</td>
                            <td class="td-padding">{{ document.name }}</td>
                            <td class="td-padding">{{ document.number }}</td>
                            <td class="td-padding">{{ document.documentType }}</td>

                            <td class="text-right">
                                <div class="btn-group">
                                    <a href="{{ path('document.edit', {id: document.id}) }}" class="btn btn-primary btn-sm color-white" data-toggle="tooltip" data-placement="top" title="Edit"><i class="fa fa-edit"></i></a>
                                    <a href="{{ path('document.delete', {id: document.id} ) }}" class="btn btn-danger btn-sm btn-delete-post color-white" data-toggle="tooltip" data-placement="top" title="Delete"><i class="text-light fa fa-trash"></i></a>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}

                        </tbody>
                    </table>

                    <div class="pull-left" style="width:200px;margin-bottom: 10px;">
                        <span style="display:inline-block">Pokaż</span>
                        <div style="display:inline-block">
                            <select class="form-control" style="height: 22px; font-size: 12px; padding: 0px 0px; display: inline-block;">
                                <option>25</option>
                                <option>50</option>
                                <option>100</option>
                                <option>250</option>
                                <option>500</option>
                            </select>
                        </div>
                        pozycji z 1034
                    </div>

                    <div class="pull-right">
                        <div class="btn-group">
                            <button type="button" class="btn btn-white btn-xs"><< Pierwsza</button>
                            <button class="btn btn-white btn-xs">< Poprzednia</button>
                        </div>
                        <div class="btn-group form-inline" style="display:inline-block; font-size: 12px; margin-top: 0px;">
                            Strona:
                            <input type="text" class="form-control" value="5" style="height: 20px; width: 40px; font-size: 12px; padding: 0px 3px; text-align: center;"> z 34
                        </div>
                        <div class="btn-group">
                            <button type="button" class="btn btn-white btn-xs">Następna ></button>
                            <button class="btn btn-white btn-xs">Ostatnia >></button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}

{% block javascripts %}

    <script src="{{ asset('assets/js/main.js') }}"></script>

{% endblock %}

